#ifndef FUNCTION_H
#define FUNCTION_H

/* Sometimes we want to keep the parameters to a functin for later use
 * and this is a solution to avoid the 'unused parameter' compiler warning */
#define UNUSED(x) (void)(x)

#endif